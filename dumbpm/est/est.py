from typing import List

from pandas import DataFrame


def estimate(
    scope: int,
    velocity: List[float],
    change: List[float],
    normal: bool,
    simulations: int,
) -> DataFrame:
    """Estimate."""
    return DataFrame()
